import Image from 'next/image';
import React from 'react';

const TikTokIcon = () => {
  return <Image src='/Images/tiktok.svg' width={30} height={30} className=' bg-white' />;
};

export default TikTokIcon;
